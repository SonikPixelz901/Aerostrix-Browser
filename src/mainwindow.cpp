#include "mainwindow.h"

MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
{
    setWindowTitle("Aerostrix Browser");
    resize(1024, 768);
    // Setup UI components later here
}

MainWindow::~MainWindow() {}
